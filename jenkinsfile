pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Code checked out successfully'
            }
        }

        stage('Build') {
            steps {
                sh 'ls -la'
            }
        }
    }
}
